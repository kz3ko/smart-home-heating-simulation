version: "3.9"

services:

  backend:
    restart: always
    build: ./backend
    container_name: backend
    command: python app.py
    volumes:
      - ./backend:/backend
      - type: bind
        source: ./house-config.json
        target: /backend/config/house-config.json
        read_only: true
    ports:
      - '8000:8000'

  frontend:
    restart: always
    build: ./frontend
    container_name: frontend
    command: npm start
    volumes:
      - ./frontend:/frontend
      - /frontend/node_modules
      - type: bind
        source: ./house-config.json
        target: /frontend/src/house-config.json
        read_only: true
    ports:
      - '3000:3000'
    depends_on:
      - backend
